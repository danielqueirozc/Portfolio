import { Heebo, Kalam } from "next/font/google";

export const heebo = Heebo({
    weight: ['400','500', '700'],
    subsets: ["latin"] 
   });
 
 export const kalam = Kalam({
     weight: ['400'],
     subsets: ["latin"] 
    });